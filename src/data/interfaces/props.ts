export interface sitewayProps {
  title: string;
  url: string;
}

export interface questionTypes {
  active: boolean;
  title?: string;
  file?: null | File;
  desc?: string;
}

export interface siteWayTypes {
  title: string;
  url: string;
}
